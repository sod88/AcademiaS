global class DailyLeadProcessor implements Schedulable{
    global void execute(SchedulableContext ctx){
        List<Lead> lLista = [Select Id, LeadSource from Lead where LeadSource = null];
        
        if(!lLista.isEmpty()) {
            for(Lead l: lLista) {
                l.LeadSource = 'Dreamforce';
            }
            update lLista;
        }
    }
}