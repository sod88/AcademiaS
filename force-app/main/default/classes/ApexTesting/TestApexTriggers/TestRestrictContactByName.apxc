@isTest
public class TestRestrictContactByName {
    static testMethod void testInsert(){
        Contact con = new Contact(LastName = 'INVALIDNAME', FirstName = 'Sebastian');
        Test.startTest();
        try{
            insert con;
        }catch(Exception e){
            Test.stopTest();
            System.debug(e.getMessage());
        }
    }
}